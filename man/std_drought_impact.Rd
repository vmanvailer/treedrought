% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/std_drought_impact.R
\name{std_drought_impact}
\alias{std_drought_impact}
\title{Standardized Drought Impact Analysis}
\usage{
std_drought_impact(
  chron_data,
  chron_group_col = NULL,
  clim_data,
  clim_growth_end = c(NH = 8, SH = 2),
  clim_growth_period = 12,
  clim_spei_scale = 1,
  clim_rescale_spei = TRUE,
  thr_pointer_year_prop_sites = 0.3,
  thr_multi_drought_tiebreak = 0.65,
  n_years_baseline = 2,
  n_years_recovery = 2,
  model_min_n_drought_events = 3,
  model_resistance_val = 0.5
)
}
\arguments{
\item{chron_data}{data.table or data.frame of tree-ring data.
Must contain columns \code{Id}, \code{Year}, and either \code{RWI} or \code{RES}.}

\item{chron_group_col}{Character vector naming the column(s) to group sites for regional drought detection
(e.g. \"ADMIN_GROUPING\", \"CLUSTER\"); if NULL, all sites are treated as one group.}

\item{clim_data}{data.table or data.frame of climate series.  Must contain \code{Id}, \code{Lat}, \code{Year}, \code{Month}, \code{TAve}, \code{Prec}.}

\item{clim_growth_end}{Named numeric vector c(NH=8, SH=2) indicating the final month of the growth year
in each hemisphere (default 8=Aug for NH, 2=Feb for SH).}

\item{clim_growth_period}{Integer length of the growth year in months (default 12).}

\item{clim_spei_scale}{Integer scale for SPEI calculation (default 1 = 1-month SPEI).}

\item{clim_rescale_spei}{Logical; if TRUE (default), will z-score SPEI per site after calculation.}

\item{thr_pointer_year_prop_sites}{Numeric in \link{0,1}; minimum proportion of sites in a group
required to flag a pointer (drought) year (default 0.3).}

\item{thr_multi_drought_tiebreak}{Numeric in \link{0,1}; proportion threshold for resolving
mixed “immediate” vs “delayed” drought responses (default 0.65).}

\item{n_years_baseline}{Integer number of pre-drought years to include (default 2).}

\item{n_years_recovery}{Integer number of post-drought years to include (default 2).}

\item{model_min_n_drought_events}{Integer minimum distinct drought events per group to fit the recovery model (default 3).}

\item{model_resistance_val}{Numeric resistance value at which to compare modeled recovery vs full resilience (default 0.5).}
}
\value{
A named list with two elements:
\describe{
\item{intermediate_steps}{A list of all intermediate data.tables and parameters for full reproducibility.}
\item{predicted_recovery}{A data.table of group, site Id, and the mean and SE of projected growth reduction
at the specified resistance (\code{model_resistance_val}).}
}
}
\description{
Orchestrates the full pipeline for calculating, modeling, and summarizing
standardized drought impacts on tree-ring chronologies.  This wrapper function:
\enumerate{
\item Aligns climate data to growth years and computes SPEI
\item Optionally imputes missing ring-width/index data
\item Merges climate and chronology
\item Detects site-level drought events
\item Defines regional drought years based on a user-specified group
\item Expands each drought event window to include pre- and post-drought years
\item Calculates resistance, recovery, and resilience indices
\item Fits a negative-exponential recovery curve, bootstraps it,
and evaluates recovery relative to the ideal “full resilience” line
}
}
\examples{
\dontrun{
# run full standardized drought impact pipeline
result <- std_drought_impact(
  chron_data            = my_ring_data,
  chron_group_col       = c(\"ADMIN_GROUPING\",\"CLUSTER\"),
  clim_data             = my_climate_data
)
# view final projected recovery table
result$predicted_recovery
}

}
